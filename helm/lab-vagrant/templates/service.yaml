apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab-vagrant.fullname" . }}
  labels:
    {{- include "lab-vagrant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab-vagrant.selectorLabels" . | nindent 4 }}
