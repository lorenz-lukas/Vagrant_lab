apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab-vagrant.fullname" . }}-test-connection"
  labels:
    {{- include "lab-vagrant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab-vagrant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
